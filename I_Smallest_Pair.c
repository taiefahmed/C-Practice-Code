#include <stdio.h>
int main()
{
    int i, j;

    scanf("%d", &i);
    scanf("%d", &j);
    int arr[j];
    for (int i = 0; i < j; i++)
    {     
        scanf("%d", &arr[j]);
    }
   
    return 0;
}